# Word工具箱安装说明

本文档面向最终用户，说明当前版本的真实支持范围，以及应该如何选择安装包。

## 1. 当前支持矩阵

- 64 位 Word：支持
- 32 位 Word：暂不支持
- 32 位 WPS：暂不支持
- 64 位 WPS：暂不支持

## 2. 安装包说明

当前交付仍包含两个安装包：

- `x64 安装包`
- `x86 安装包`

请注意：

- `x64 安装包` 仅面向 `64 位 Microsoft Word`
- `x86 安装包仅用于展示当前不支持的位数提示`

也就是说，当前版本只有 `64 位 Microsoft Word` 是正式支持的目标环境。

## 3. 系统要求

- Windows 10 / 11
- 64 位 Microsoft Word 桌面版
- `.NET Framework 4.8`
- 管理员权限

不支持：

- 32 位 Word
- 32 位 WPS
- 64 位 WPS
- Word 网页版
- Office Online
- macOS
- 移动端 WPS

## 4. 如何判断 Word 是否为 64 位

在 Word 中打开：

`文件 -> 账户 -> 关于 Word`

在版本信息里查看是否标注为 `64 位`。

只有确认是 `64 位 Microsoft Word`，才建议继续安装当前版本。

## 5. 正式安装步骤

1. 完全关闭 Microsoft Word，包括后台进程。
2. 运行 `x64 安装包`。
3. 按安装向导完成安装。
4. 安装完成后，重新打开 64 位 Microsoft Word。

## 6. 安装后的检查方式

打开：

`文件 -> 选项 -> 加载项`

在底部“管理”中选择 `COM 加载项`，点击“转到...”，确认 `Word工具箱` 已启用。

## 7. 不支持环境的提示

如果你使用的是以下任一环境：

- 32 位 Word
- 32 位 WPS
- 64 位 WPS

当前版本都会给出明确提示，告诉你该环境暂不支持。  
请不要继续尝试在这些环境中安装或手动注册当前版本。

## 8. 手动注册（仅限维护人员）

仓库中仍保留了手动注册脚本：

- `RegisterPlugin.ps1`
- `RegisterPlugin.bat`

但它们当前同样只面向 `64 位 Microsoft Word`。  
如果尝试传入 `x86`、`WPS` 或其他不受支持的宿主 / 位数，脚本会直接给出“不支持”提示并退出。
