[pytest]
pythonpath = .
testpaths = tests
addopts = -m "not integration and not ui_integration"
markers =
    integration: tests that invoke real PowerShell/Office on the host machine
    ui_integration: FlaUI/UIA tests that drive WinForms and require Word + registered add-in
